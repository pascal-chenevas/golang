package main

import (
	"fmt"
	s "strings"
)

func main() {

	input := ""

	for {
		fmt.Printf("Your text: ")
		fmt.Scan(&input)

		if s.Contains(input, "i") || s.Contains(input, "a") || s.Contains(input, "n") {
			fmt.Println("Found!")
		} else {
			fmt.Println("Not Found!")
		}
	}
}
